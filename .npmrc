# Hoist native modules that need to be resolved by Vitest/vite-node
# re2 is a dependency of @mcpambassador/core but needs to be accessible
# from the server package's test runner context
public-hoist-pattern[]=re2
